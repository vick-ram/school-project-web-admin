interface Inventory {
  productId: string;
  name: string;
  description: string;
  unitPrice: string;
  image: string;
  stock: number;
  reorderLevel: number;
  sku: string;
  createdAt: string;
  updatedAt: string;
}
